import { Placeholder } from "@/components/layout/Placeholder";

export default function AnomaliesPage() {
  return (
    <Placeholder
      title="Anomalias"
      subtitle="Detecção via z-score (|z| > 2) em KPIs core"
      phase="Fase 3"
    />
  );
}
